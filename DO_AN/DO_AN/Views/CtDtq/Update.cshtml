@model DO_AN.Models.CtDtq

@{
    ViewData["Title"] = "Update";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<style>
    .container {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        max-width: 100%;
        padding: 0 15px;
    }

    .table-bordered {
        border: 2px solid #000;
    }

        .table-bordered thead th {
            border: 2px solid #000;
        }

    /* Loại bỏ hiệu ứng màu khi hover */
    a:hover {
        color: inherit !important;
    }
</style>
<h1>Chỉnh sửa Điểm Đến</h1>

<hr />
<div class="row">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th colspan="2" style="border: 2px solid #000;">Chỉnh sửa Điem tham quan</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>
        <form asp-action="Update">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Madtq" />
            <input type="hidden" asp-for="Madl" />
            <div class="form-group">
                <label asp-for="Motachitiet" class="control-label">Mô tả </label>
                <textarea asp-for="Motachitiet" class="form-control" ></textarea>
                <span asp-validation-for="Motachitiet" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
        </td>
        </tr>
        <tr>
            <td colspan="2">
                <a class="btn btn-default" asp-action="Index">Back to List</a>
            </td>
        </tr>
        </tbody>
        </table>
    </div>

    @section Scripts {
        @{
            await Html.RenderPartialAsync("_ValidationScriptsPartial");
        }
    }


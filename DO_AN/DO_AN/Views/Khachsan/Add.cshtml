@model DO_AN.Models.Khachsan

@{
    ViewData["Title"] = "Add";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<style>
    .container {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        max-width: 100%;
        padding: 0 15px;
    }

    .table-bordered {
        border: 2px solid #000;
    }

        .table-bordered thead th {
            border: 2px solid #000;
        }

    a:hover {
        color: inherit !important;
    }
</style>

<div class="container">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th colspan="2" style="border: 2px solid #000;">Thêm Khách Sạn</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>
                    <form asp-action="Add">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="form-group">
                            <label asp-for="Madd" class="control-label">Tỉnh thành phố</label>
                            <select asp-for="Madd" class="form-control" asp-items="ViewBag.Diemdens"></select>
                        </div>
                        <div class="form-group">
                            <label asp-for="Tenks" class="control-label">Tên khách sạn</label>
                            <input asp-for="Tenks" class="form-control" />
                            <span asp-validation-for="Tenks" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="Dc" class="control-label">Địa chỉ</label>
                            <input asp-for="Dc" class="form-control" />
                            <span asp-validation-for="Dc" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <input type="submit" value="Create" class="btn btn-default" />
                        </div>
                    </form>
                </td>
            </tr>
            <tr>
                <td colspan="2">
                    <a class="btn btn-default" asp-action="Index">Quay lại danh sách</a>
                </td>
            </tr>
        </tbody>
    </table>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
